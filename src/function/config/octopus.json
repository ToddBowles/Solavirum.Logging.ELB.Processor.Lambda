{
    "type" : "logs",
    "sourceModuleName" : "ELB",
    "environment" : "#{Octopus.Environment.Name}",
    "component" : "#{Component}",
    "application" : "#{Application}",
    "logstashHost" : "#{LogHost}",
    "logstashPort" : #{LogPort},
    "connectionCountLimit" : #{Connections.Limit},
    "connectionWaitMilliseconds" : #{Connections.WaitMilliseconds}
}